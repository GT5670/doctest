. From the *CI Provider* drop-down list, select the appropriate supported Continuous Integration (CI) tool (for example, *Jenkins* or *Tekton*) that the system will use to build, test, and deploy the application.
